import { BlacktextDirective } from './blacktext.directive';

describe('BlacktextDirective', () => {
  it('should create an instance', () => {
    const directive = new BlacktextDirective();
    expect(directive).toBeTruthy();
  });
});

window.onload = function () {


let element = document.getElementById('top');
console.log(element);
if (element.scrollHeight + element.scrollTop === element.clientHeight)
    {
      document.getElementsByTagName('a').color ="white"
    }
}
